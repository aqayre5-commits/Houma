| ID | severity | file path | neighborhood | city | service | expected behavior | actual behavior | blocker yes/no | fix direction |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AA-01 | high | `content/neighborhoods.ts`; `app/villes/[city]/quartiers/[neighborhood]/page.tsx`; `app/villes/[city]/quartiers/[neighborhood]/demarches/[service]/page.tsx` | all unsupported or unmodeled neighborhoods | Casablanca, Rabat, Tanger | all | Every supported neighborhood should map to a responsible office or an explicit city-level fallback, and unsupported neighborhoods should degrade to a city fallback experience. | Neighborhood records are generated only from existing office districts, so any neighborhood outside those derived keys simply does not exist; both neighborhood routes call `notFound()` when `getNeighborhood()` misses, so the user gets a 404 instead of a city fallback. | yes | Add explicit neighborhood records and fallback-capable neighborhood states; route missing neighborhoods to a city-level fallback module instead of `notFound()`. |
| AA-02 | blocker | `content/offices.ts`; `lib/resolver.ts` | all neighborhoods | Casablanca, Rabat, Tanger | `livret-famille`; `acte-deces`; `duplicata-cin`; `permis-construire`; `inscription-liste-electorale` | Every `local_jurisdiction` service should resolve differently at neighborhood level than at city-only level wherever neighborhood routing is exposed. | These services are modeled as local-jurisdiction elsewhere in the app, but no office records expose them in `supportedServices`, so neighborhood-level resolution cannot identify a responsible office and falls back to generic city behavior. | yes | Add office coverage or explicit city-fallback policies for each local-jurisdiction service, and validate that every local-jurisdiction service has neighborhood-aware resolution support before exposing neighborhood routes. |
| AA-03 | high | `content/offices.ts`; `content/neighborhoods.ts`; `lib/resolver.ts` | `centre`; `hay-hassani`; `ain-sebaa`; `yacoub-el-mansour`; `agdal-riyad`; `beni-makada`; `malabata` | Casablanca, Rabat, Tanger | all office-sensitive services | Neighborhood-to-office mapping should be based on actual annex/caïdat/arrondissement responsibility, not a district-wide one-to-one shortcut. | `neighborhoodsServed` is always `[districtSlug]`, and neighborhood records are derived directly from those district slugs, so the resolver can only do district-level matching. There is no finer-grained catchment model and no distinction between “this office is in the district” and “this office is responsible for this neighborhood.” | yes | Replace district-equals-neighborhood derivation with explicit neighborhood coverage arrays per office, and resolve by confirmed catchment rather than shared district slug. |
| AA-04 | medium | `content/offices.ts`; `app/bureaux/[office]/page.tsx`; `components/office-card.tsx` | all | Casablanca, Rabat, Tanger | all | Office records should expose office type, served neighborhoods, supported services, city, and fallback behavior in both data and user-facing office detail surfaces. | Data records include `officeType`, `citySlug`, `neighborhoodsServed`, and `supportedServices`, but no office sets `fallbackOfficeSlug` or any explicit fallback behavior. The office detail page also does not surface office type or fallback handling to the user. | no | Populate explicit office fallback metadata where needed, and render office type/fallback status on office detail pages so responsibility is inspectable rather than implicit. |
| AA-05 | medium | `app/villes/[city]/quartiers/[neighborhood]/page.tsx`; `content/neighborhoods.ts` | all modeled neighborhoods | Casablanca, Rabat, Tanger | all | A neighborhood hub should show neighborhood-specific local guidance, responsible-office context, and supported service subset. | The neighborhood hub is mostly a thin shell: heading, SEO description, and the first office card only. It does not list supported services for that neighborhood, does not explain fallback behavior, and does not show why the displayed office is the responsible one. | no | Add neighborhood-specific service subset rendering, explicit responsibility text, and a visible city fallback path when local coverage is partial. |
| AA-06 | high | `app/villes/[city]/quartiers/[neighborhood]/demarches/[service]/page.tsx`; `lib/resolver.ts` | all modeled neighborhoods | Casablanca, Rabat, Tanger | all neighborhood-routed services | A neighborhood service page should materially differ from the city-level service page by adding final local routing, while still preserving service requirements, fees, delay, and fallback guidance. | The neighborhood service page only renders the service title, one resolver message, one office card when available, and sources. It drops requirements, fees, delay, and structured fallback guidance, so the route is effectively a thin local shell rather than a full neighborhood-aware service page. | yes | Render the full service stack on neighborhood routes and layer the local responsibility module on top, rather than replacing core service content with a short resolver message. |
| AA-07 | medium | `app/villes/[city]/quartiers/[neighborhood]/page.tsx`; `app/villes/[city]/quartiers/[neighborhood]/demarches/[service]/page.tsx`; `components/office-card.tsx` | all modeled neighborhoods | Casablanca, Rabat, Tanger | all | Office cards and neighborhood service pages should use responsibility language consistently and avoid implying that the displayed office is merely a convenient nearby option. | The resolver messages use responsibility-oriented wording, but the neighborhood hub itself simply shows `officeSlugs[0]` with no explanatory responsibility banner. `OfficeCard` also renders generic practical text and actions without stating why that office is responsible for the neighborhood. | no | Add a responsibility banner or subtitle sourced from resolver/context on neighborhood pages and office cards, so the office is framed as “responsible for this area” rather than just “shown on this page.” |
| AA-08 | medium | `lib/content-validation.ts`; `content/offices.ts`; `content/neighborhoods.ts`; `lib/resolver.ts` | all | Casablanca, Rabat, Tanger | all | Validation should fail when a supported neighborhood lacks office/fallback coverage, when a local-jurisdiction service has no neighborhood-capable mapping, or when an online-only service accidentally exposes office routing. | Validation only checks referential integrity and missing references. It does not enforce annex-awareness rules, fallback coverage completeness, or neighborhood-level service resolvability. | no | Expand content validation to assert neighborhood coverage completeness, per-service local resolution capability, and online-first no-office behavior before build succeeds. |
