| Rank | ID | Area | Severity | Release blocker | File path(s) | Route(s) | Reproduction steps | Expected result | Actual result | Risk | Recommended fix direction | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | SUM-01 | SEO / Release readiness | blocker | yes | `lib/seo.ts:3-37`; `app/robots.ts`; `app/sitemap.ts` | `/`; `/robots.txt`; `/sitemap.xml`; all canonicalized routes | 1. Run `npm run build`. 2. Start the app. 3. Fetch `/robots.txt`, `/sitemap.xml`, and any HTML route head. | Canonical URLs, sitemap URLs, and robots sitemap pointer should use the production domain. | Runtime output uses `https://example.com` for canonicals, sitemap entries, and robots sitemap pointer. | Search engines can index the wrong host, split ranking signals, and generate broken SERP links. | Set `NEXT_PUBLIC_SITE_URL` in every environment and fail fast when missing instead of silently falling back to `example.com`. | high |
| 2 | SUM-02 | Bilingual integrity / UX / Accessibility | blocker | yes | `app/demarches/[service]/page.tsx:13-149`; `app/a-propos/page.tsx:4-64`; `app/methodologie/page.tsx:4-67`; `app/sources/page.tsx:6-233`; `components/trust-strip.tsx:3-37`; `components/location-detector.tsx:168-309` | `/demarches/{slug}`; `/a-propos`; `/methodologie`; `/sources`; `/recherche`; `/bureaux`; `/guides/{slug}`; `/404` under `qriba_lang=ar` | 1. Set cookie `qriba_lang=ar`. 2. Open `/demarches/passeport-marocain`, `/a-propos`, `/methodologie`, `/sources`. 3. Inspect rendered copy and metadata. | Arabic locale should render Arabic UI copy, metadata, headings, CTAs, and trust content consistently. | Document `lang/dir` switch to Arabic, but major route bodies, headings, breadcrumbs, metadata, and trust blocks remain French; AR vignette CTAs stay French. | Core FR↔AR journey is broken, creates mixed-language pages, hurts trust, and degrades screen-reader pronunciation because French content is announced under `lang="ar"`. | Localize route bodies and metadata from a single locale-aware content layer; cover static pages, service pages, guides, search, 404, trust strip, and location detector. | high |
| 3 | SUM-03 | Core flow / Service integrity | blocker | yes | `app/villes/[city]/demarches/[service]/page.tsx:97-130`; `lib/data.ts:265-278`; `lib/data.ts:1364-1428` | `/villes/casablanca/demarches/vignette-auto`; `/villes/rabat/demarches/vignette-auto`; `/villes/tanger/demarches/vignette-auto` | 1. Open any city vignette route. 2. Read section 1 and the fee presentation. 3. Repeat in Arabic. | Vignette should present an online-only flow with no bureau guidance and a clear 8-band rate table in FR and AR. | Page renders `Bureau non confirmé` with commune/prefecture fallback, even though fallback note says no travel is needed; rates are buried in one long paragraph and there is no table. | Users can be misdirected to an office for a no-visit service and cannot scan the national rate bands accurately. | Special-case online-only services like vignette so they render an online flow module instead of office fallback; replace dense rate prose with a semantic FR/AR table. | high |
| 4 | SUM-04 | Application integrity / CI | blocker | yes | `package.json:5-30`; `tests/smoke.spec.ts:5-190` | repo-wide | 1. Run `npm run lint`. 2. Run `npm run test:e2e`. | Validation commands should execute successfully and reflect the live UI. | `npm run lint` fails immediately because `next lint` is invalid in the installed Next version; `npm run test:e2e` fails on a syntax error at `tests/smoke.spec.ts:55`, and the suite also contains stale selectors/content. | Release validation is non-functional; regressions can ship without lint or end-to-end protection. | Replace the obsolete lint command, repair the broken test file, add working Playwright config/baseURL wiring, and update assertions to the current UI. | high |
| 5 | SUM-05 | Search / Core user flow | high | no | `components/search-form.tsx:8-57`; `lib/data.ts:1476-1498`; `app/recherche/page.tsx:18-45` | `/`; `/recherche` | 1. Inspect homepage. 2. Search the codebase for `SearchForm`. 3. Open `/recherche?q=passeport%20Casablanca`. | Homepage should expose a search entry point and combined city+service queries should resolve as suggested by the placeholder copy. | `SearchForm` is not mounted anywhere, so homepage search is absent; the placeholder promises `passeport Casablanca`, but the query returns `Aucun résultat direct`. | One of the required core journeys, homepage/search → service, is not actually available from the homepage and the existing search UX overpromises capability. | Mount a real search entry point and tokenize/rank multi-term queries so city+service combinations resolve predictably. | high |
| 6 | SUM-06 | Content integrity / Trust | high | no | `app/a-propos/page.tsx:23-41`; `app/methodologie/page.tsx:23-54` | `/a-propos`; `/methodologie` | 1. Open `/a-propos` and `/methodologie`. 2. Compare listed services with `lib/data.ts` service set. | Pages claiming `17 démarches` should list the full, current 17-service scope. | `À propos` omits `livret-famille`; `Méthodologie` omits `renouvellement-cnie`, `duplicata-cin`, and `livret-famille` while still claiming 17 services. | Scope statements are stale and reduce trust in the site’s verification claims. | Generate scope lists from the service data source instead of hand-maintaining route copy. | high |
| 7 | SUM-07 | Analytics instrumentation | high | no | `components/page-view-tracker.tsx:6-12`; `components/search-form.tsx:26-43`; `app/villes/[city]/demarches/[service]/page.tsx:145-178`; `app/sources/page.tsx:187-221` | Most routes; outbound source links; online CTAs; search; language switch | 1. Audit all `track(...)` calls in code. 2. Compare them against required page views, search, language, outbound source, CTA, and sponsor/ad events. | Core routes and key actions should all emit stable analytics events. | Only service, city-service, office page views plus maps/forms/location are instrumented; home, city, index, search, guides, static pages, source links, online links, language switch, and card CTAs are missing. Search events are defined only in an unused component. | Funnel and trust-link analytics are incomplete, making performance and conversion decisions unreliable. | Add route-wide page views and instrument all core CTAs/outbound trust links from mounted components only. | high |
| 8 | SUM-08 | Application integrity / Location UX | high | no | `components/location-detector.tsx:95-150`; `app/api/detect-location/route.ts:6-77` | `/`; `/api/detect-location` | 1. Call `/api/detect-location` in the running app. 2. Trigger homepage location detection. | IP fallback should return a covered city or a controlled local-network/off-coverage state. | Current runtime returned `{"ok":false,"reason":"api_error"}` from `/api/detect-location`, which leads the homepage detector into the failed state. | Homepage’s “nearby bureau” feature is brittle and can collapse into failure if the upstream lookup rejects the request. | Harden upstream error handling, cache provider results, and degrade to a deterministic manual city chooser instead of an opaque failure. | medium |
| 9 | SUM-09 | Forms / Trust | high | no | `app/api/forms/error-report/route.ts:4-26`; `lib/supabase.ts:1-8` | `/signaler-une-erreur`; `/api/forms/error-report` | 1. Confirm no visible Supabase env vars are set. 2. POST valid JSON to `/api/forms/error-report`. | The endpoint should either persist the submission or return a storage/configuration error. | The endpoint returns `{ok:true,id}` even when no Supabase client is configured and no insert is attempted. | Users can believe a correction was recorded when it was silently discarded. | Fail closed when storage is unavailable and confirm insert success before returning 200. | medium |
| 10 | SUM-10 | Performance / Caching | high | no | `app/layout.tsx:27-42`; build output from `npm run build` | All HTML routes | 1. Run `npm run build`. 2. Inspect route classification. 3. Inspect root layout. | Data-driven informational pages should be statically generated or cacheable where possible. | Build output marks essentially every HTML route as dynamic (`ƒ`) because `getLang()` reads cookies in the root layout. | Cacheability, TTFB, and crawl efficiency are weaker than necessary for a content-heavy site. | Move locale resolution out of the root layout or use path-based locale routing so static pages can prerender. | high |
| 11 | SUM-11 | Route correctness / Data mapping | high | no | `lib/data.ts:297-509`; `app/villes/[city]/demarches/[service]/page.tsx:38-41`; `app/villes/[city]/demarches/[service]/page.tsx:103-117` | 18 city-service routes for `livret-famille`, `acte-deces`, `duplicata-cin`, `permis-construire`, `inscription-liste-electorale`, `vignette-auto` | 1. Compare `services` to office `services` arrays. 2. Open city-service routes for the listed services. | Local guide pages should recommend a real office or intentionally render a non-office flow. | Six services have no office mapping at all, so every affected city-service route falls back to a generic `Bureau non confirmé` state. | The core “find the right local office” promise is weakened on 6/17 services. | Add explicit office/fallback models per service instead of relying on office-array lookup alone. | high |
| 12 | SUM-12 | Source / Trust-link verification | low | no | `app/sources/page.tsx:12-153`; `lib/data.ts:51-291`; external source URLs | `/sources`; service detail outbound links | 1. Extract unique official URLs. 2. Probe them from the audit environment. | All official links should be verified or clearly marked as unverified when runtime validation is inconclusive. | Some sources resolved (`watiqa.ma`, `casablancacity.ma`, `tanger.ma`, `v2khadamatnarsa.ltc.ma`); several others were blocked, timed out, or rejected automated fetches from the audit environment (`passeport.ma`, `dgsn.ma`, `idarati.ma`, `portail.ma`, `mavignette.ma`, `ompic.ma`). | Trust-link verification is only partial; some statuses are environment-blocked rather than conclusively broken. | Re-run link validation from production CI with a browser-capable HTTP checker and record status per source. | medium |
