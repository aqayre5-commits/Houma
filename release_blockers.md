| Rank | ID | Release blocker | Why it blocks release | File path(s) | Route(s) | Evidence / reproduction | Recommended fix direction | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | RB-01 | Wrong canonical/robots/sitemap host | Production SEO surfaces currently emit `https://example.com`, which is unsafe to ship. | `lib/seo.ts`; `app/robots.ts`; `app/sitemap.ts` | Site-wide | Fetch `/robots.txt`, `/sitemap.xml`, or any page head in the running app. | Require and validate `NEXT_PUBLIC_SITE_URL`; remove `example.com` fallback in release builds. | high |
| 2 | RB-02 | Arabic core journey incomplete | The product exposes an Arabic switch, but core routes and metadata remain French. | `app/demarches/[service]/page.tsx`; `app/a-propos/page.tsx`; `app/methodologie/page.tsx`; `app/sources/page.tsx`; `components/trust-strip.tsx`; `components/location-detector.tsx` | AR route views | Set `qriba_lang=ar` and open the audited routes. | Complete locale coverage or narrow the exposed locale surface before release. | high |
| 3 | RB-03 | Vignette route contradicts itself | Online-only service still renders office fallback and lacks a scannable rate table. | `app/villes/[city]/demarches/[service]/page.tsx`; `lib/data.ts` vignette entries | All 3 vignette city routes | Open any vignette city route in FR/AR and inspect section 1 + fee rendering. | Give vignette a dedicated online-only presentation and semantic rate table. | high |
| 4 | RB-04 | Validation pipeline broken | Lint and E2E commands do not run, so release checks are not trustworthy. | `package.json`; `tests/smoke.spec.ts` | repo-wide | Run `npm run lint` and `npm run test:e2e`. | Repair scripts/config/tests before further implementation work. | high |
