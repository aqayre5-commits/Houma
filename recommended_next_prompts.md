| Rank | Safe follow-up prompt | Why this should come next | Expected output from that prompt | Dependencies / notes |
| --- | --- | --- | --- | --- |
| 1 | `Stabilize the validation pipeline only: pin dependency versions, replace the broken lint command, repair Playwright config/tests, and make build/lint/test all pass. Do not change product copy or features yet.` | Restores confidence in every later implementation step. | Working `build`, `lint`, and `test:e2e` commands plus updated test coverage. | Do this before product fixes so regressions are catchable. |
| 2 | `Fix release-critical SEO infrastructure only: remove the example.com fallback, wire the real site URL through metadata/robots/sitemap, and add guardrails so missing site URL fails the build.` | Prevents shipping bad canonical/indexation signals. | Correct canonical/robots/sitemap output and environment validation. | Independent of UI work. |
| 3 | `Complete bilingual parity for all existing routes and metadata without adding new features: localize route bodies, trust strips, search/static pages, 404, and metadata/hreflang for FR and AR.` | Clears the largest user-facing blocker after release SEO. | Fully localized FR/AR routes with parity audit notes. | Should include automated locale regression tests. |
| 4 | `Refactor service-flow rendering only: introduce explicit flow types (office-backed, commune-backed, online-only, hybrid) and eliminate generic office fallback where it is incorrect, starting with vignette-auto.` | Fixes the most dangerous route-model contradiction and the unmapped-service problem together. | Correct local-office behavior across service pages, especially vignette. | Use existing data; do not add new services. |
| 5 | `Improve search and analytics only: mount the real search UI, make combined city+service queries work, and instrument page views, card clicks, language switches, and official outbound links.` | Restores a required core journey and the measurement layer needed for later prioritization. | Search flow, query ranking, and analytics coverage map. | Should follow locale work so tracking names/payloads are stable. |
| 6 | `Harden supporting systems only: make error-report submissions fail closed when storage is unavailable, stabilize location detection fallbacks, and add automated external-link verification for official sources.` | Converts current trust risks into deterministic behavior. | Reliable forms, resilient location fallback, and source-link CI reporting. | Best after routing/locale/analytics stabilize. |
